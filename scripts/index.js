import { getDate, getNewId, login, register, addPost, editPost, deletePost, getPosts } from "./api.js"
import { getIndexOfId, renderHeader, renderMain, renderModal, renderLoginPage, renderRegisterPage, renderLoggedInPage } from "./render.js"

